{
    "type": "commonjs",
    "scripts": {
        "test": "jest",
        "test-watch": "jest --watchAll"
    },
    "devDependencies": {
        "jest": "^28.1.3",
        "xml2json": "^0.12.0"
    },
    "jest": {
        "verbose": false
    }
}
